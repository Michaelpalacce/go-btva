/*
The `handler` package determines which components to fetch based on the OS.

# Purpose

The `handler` package is the main driving force of the application as it's responsible for combinig most if not all
components and executing different tasks with them
*/
package orchestrator
